// time complexity is same as sets which is O(1)
//in the wordst case the tume complexity becomes o(N)

//all the operations are and work the same as sets except lower and upper bound

void unorderedSet(){
    unordered_Set<int> st;

    //1.lower_bound and upper_bound functons do not work
    //rest al lthe functions are same
    //2.as above, it does nnot store data in any particular order
    //3. it has a better time cimplexity that sets in most cases(o(1))
    //   except in the worst case where it gves o(n)
    
}